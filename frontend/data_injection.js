// data_injection.js
// Loads JSON data and injects it into components using data-slot attributes

// This site doesn't currently use data injection from JSON,
// but this file is provided for future use if needed

document.addEventListener('componentsLoaded', () => {
  // Data injection logic can be added here when needed
  console.log('Components loaded, data injection ready');
});
